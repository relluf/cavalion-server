{
  "name": "cavalion-server-docker-compose",
  "version": "1.0.0",
  "description": "Installs and runs cavalion-server in a Docker container",
  "scripts": {
  	"start": "bash install",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "ralph",
  "license": "ISC"
}
